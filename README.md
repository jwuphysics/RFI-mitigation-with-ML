# Mitigating RFI with ML

All notebooks are in the `notebook` directory. Some CASA images are shown in the `images` directory. Data files (`FITS` or `MS` data sets) are stored in the `data` directory.
